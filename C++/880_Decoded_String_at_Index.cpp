#include<bits/stdc++.h>
using namespace std;
class Solution {
public:
    string decodeAtIndex(string s, int k) {
        int size = sizeof(s)/sizeof(s[0]);
    }
};
int main(){
    string str;
    cin>>str;
    cout<<str<<endl;
    int n = str.length();
    for(int i=0;i<n;i++){
        cout<<str[i]<<"\t";
       if(isdigit(str[i])==true){
        cout<<str[i]<<endl;
       }
    }
    cout<<endl<<n<<endl;
    Solution s;
    // int k;
    // cin>>k;
    
    // cout<<s.decodeAtIndex(str,k)<<endl;
}